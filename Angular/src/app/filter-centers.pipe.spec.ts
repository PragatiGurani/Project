import { FilterCentersPipe } from './filter-centers.pipe';

describe('FilterCentersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCentersPipe();
    expect(pipe).toBeTruthy();
  });
});
