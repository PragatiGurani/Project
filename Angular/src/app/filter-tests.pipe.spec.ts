import { FilterTestsPipe } from './filter-tests.pipe';

describe('FilterTestsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTestsPipe();
    expect(pipe).toBeTruthy();
  });
});
